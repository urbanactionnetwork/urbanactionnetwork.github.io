# 소개 페이지

## '함께하는 사람들' 섹션

- 그룹화 방식 변경: 사람들을 group 값으로 묶어 섹션/헤딩별로 그룹 단위로 표시
  - 그룹(group) 필드 필수화: 모든 구성원 데이터는 반드시 `group` 값을 가져야 함

### 표시 항목 수정
- 아바타 이미지 제거
- 메일 데이터가 없으면 미표시
- 소속 항목 추가
- 직책(position) → 그룹(group)으로 변경, 그룹 값 노출
- Deprecated 제거: 데이터/타입/UI에서 `position` 필드 및 관련 폴백 로직 완전 제거